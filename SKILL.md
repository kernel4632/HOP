---
name: hop
description: 在项目的任何阶段加载并始终生效：架构设计、制定计划、编写代码、重构优化、代码审查、补注释、调试修复、文件整理。确保所有代码产出遵循 HOP（面向人类编程）思想。
---

# HOP 规范

代码要让人一眼看懂在做什么。人打开文件就知道这段代码的作用，想改就直接改，改完不担心会出问题。HOP 的唯一目标：人可以凭直觉控制代码。

---

## 1. 业务逻辑优先

人打开代码是为了看业务做什么，所以文件里应该全是业务逻辑。数据转换、异常处理、空值检查这些不是业务逻辑，不应该混在业务代码之间。

整个项目的架构要保证业务代码是纯粹的。非业务的代码要么用库和框架解决，要么提取并做通用封装成可被多处复用的工具函数放到工具目录。业务代码文件里只能看到业务步骤。

比如一个发送消息的函数，打开后只看到：存储用户消息 → 调用模型 → 发送回复 → 更新状态。文件里看不到任何检查、转换、兜底的代码。

---

## 2. 解决根本

遇到问题时，大部分人会写代码去处理这个问题。正确的做法是改变架构，让问题不会发生。问题不发生了，代码自然就少了。项目应该越做代码越少。

出现 bug 时，先找到原因，改代码或改架构让 bug 不可能出现。不要写补丁代码和胶水逻辑。只有增加功能才应该增加代码，修复问题不应该增加代码。

比如一个函数有时会收到 null 导致崩溃。错误做法是写代码判断参数是不是 null。正确做法是找到为什么会传入 null，改上游让它不会传 null，或者重新设计数据流让 null 本身是正常的值。

---

## 3. 信任数据

项目内部的代码之间要互相信任。函数接收数据后直接使用，不检查、不判断、不处理异常。数据有问题时程序报错就是最好的提示，代码全是业务逻辑时人看报错就能知道问题在哪。

内部函数之间传递数据不做任何检查，不判断参数类型，不判断是否为空，不写 try-catch。只在系统边界做检查：对外提供的 API、用户输入、外部系统返回的数据。边界检查只做一次，数据进入系统后所有内部代码都信任它。

比如 `Agent.send()` 是对外 API，检查参数不为空。内部的 `Loop.run()` 调用 `Context.build()` 时直接传数据，`Context.build()` 接收后直接使用，不再判断。

---

## 4. 逻辑集中

理解一个功能应该只需要看一个地方。相关的代码必须写在一起，看完一个文件就能理解全部逻辑。

一个主体的全部操作写在一个文件里，文件用这个主体的名字命名。不要把一个主体的功能拆成多个文件散落在不同文件夹里。某段代码只用一次，就写在使用的地方，不要提取成单独函数。只有多个地方都要用同一段代码时，才提取出去。

比如沙盒相关的操作都在 `sandbox.py` 里，预算相关的都在 `budget.py` 里，不是拆成 `execution_worker.py`、`recall_services.py` 散落各处。发送消息时检查输入不能为空，这个检查只在发送时用一次，就直接写在发送函数里使用行注释分隔，不要提取成 `validateInput()` 函数。

---

## 5. 极简命名

代码要尽可能简洁，能用一个词就不用两个词。通过代码的层级关系表达所属关系，不要在名字里重复说明。命名概念要轻，不要用沉重的技术概念词。

文件用被操作的实体主体命名，是一个名词，如 `sandbox.py`、`budget.py`，不用 `execution_worker.py`、`recall_services.py` 这种操作方法名。文件里的方法名用动词一个词。变量不加类型后缀。文件夹用小白一眼能懂的词，不用技术分层词（禁止：controllers、services、repositories、handlers、managers、processors 等）。概念要轻，写 `users` 不写 `UserRepository`。通过 `主体.方法` 表达所属关系。

```
sandbox.py          → 正确（主体名词）
execution_worker.py → 错误（操作方法名）

Context.build()         → 正确
Context.buildContext()  → 错误（context 重复了）

messages    → 正确
messageList → 错误（List 是类型后缀）

views/Chat.vue      → 正确
views/ChatView.vue  → 错误（View 重复了目录名）

users           → 正确
UserRepository  → 错误（概念过大）

tools/          → 正确（小白能懂）
repositories/   → 错误（技术分层词）
```

即使多个类都有 `build` 方法，也都只叫 `build()`，调用时通过 `Context.build()` 和 `Agent.build()` 区分。

---

## 6. 增删改方便

人觉得应该在某处改代码，改完就应该生效，不应该还需要去其他地方同步修改。如果改一个地方需要动多个文件，说明架构有问题。

让每次修改只需要动一次。删除一个功能时，只删一处代码就完成。修改一个功能时，只改一个文件。发现改一处需要同步改其他地方，就重新设计架构，重新修改写法。

加同一类新东西时要设计自动化方案：开一个文件夹，上层做自动扫描导入和注册，文件夹里只需要增加文件，文件内容按照约定格式编写，启动时自动被扫描加入，不需要显式重复写声明。删掉文件就是删掉功能，不需要清理别处。

比如要删除"消息敏感词过滤"功能，只需要删除 `filterSensitiveWords()` 函数和调用它的那一行代码。
又比如要添加llm的tools时，写一个工具文件放到 tools 目录，按约定格式编写，系统启动时自动扫描加载，移出目录即删除，不需要手动注册或注销。

---

## 7. 单一来源

同一份数据只定义一次，下游所有需要它的地方从这个来源提取，不各自重新定义。来源的结构变化时，下游自动适应，不需要逐个修改。

用统一的数据结构做单一信息来源。下游需要数据时从来源提取，结构保持相似，扩展只增加字段，不改已有结构。

`history` 和 `messages` 结构保持相似，`Context.build()` 从 `history` 提取 `messages`，后续 `history` 增加字段时提取逻辑不需要改动。

---

## 注释

注释让一个不懂这个项目的人也能读懂每一步在做什么业务。判断标准：把一个文件的代码全部删掉只留注释，读者还能知道这个文件的实现步骤与细节是什么，才算合格。

修改已有代码时，同样要给被修改的那几行补上注释，不要只在新写的代码上加。

每个文件一定要有文件头注释，使用大白话说清楚这个文件做什么的、怎么调用的、调用示例长什么样。

每个函数前和多个相近逻辑块用段落标题行注释：`// --- 添加任务 ---`

尾随注释写在代码行末，同一块内对齐到统一列，形成左代码右解说的节奏：

```js
const id = generateId()       // 生成唯一任务ID
store.tasks.push(task)        // 将任务加入全局列表
return id                     // 返回新任务ID供调用方使用
```

import 语句说明引入的是什么能力，export 说明导出了什么，卫语句说明为什么提前返回，魔法数字说明为什么是这个值，不符合直觉的写法说明为什么不用更常见的方式。

废话注释只是在复述语法（`arr.push(item) // 将 item 推入数组`），有信息量的注释说业务含义（`arr.push(item) // 将新任务加入待处理队列`）。

每个文件至少应该有50%的行注释和80%的尾随注释。

---

## 架构

所有业务走同一条路径：触发 → 指令 → 数据 → 反馈。新人从入口顺着这条链追下去，不超过三个文件就能看懂完整流程。

**入口** 只接收事件触发如监听事件、API路由触发，然后直接调用对应指令，不做任何处理与编排，只能直接传递参数。
**指令** 只做业务动作：读数据、处理、改数据、返回结果，属于主业务逻辑编排层，如果需要可以将部分主体模块拆分到功能实现层，但是要确保一个主体的全部操作都在一个文件里。
**数据** 设计上可以选择数据就集中存放和被读写，也可以选择模块主体范围内自己的状态数据就直接内部存储与内部读写，当然也可以被外部修改，按照需要灵活设计。
**工具** 放被多个业务共同使用、拿到别的项目也能用的通用性方法。

---

## 判断标准

代码给人的感觉应该像小项目。项目再大也要写得像小项目一样简单。打开文件只有业务逻辑，改功能只需要改一个地方，读代码像读流程一样顺畅。代码给人这种感觉，就是对的。

---

## 正反代码对比

正确写法：[examples/correct.js](examples/correct.js) · [examples/correct.py](examples/correct.py)
错误写法：[examples/wrong.js](examples/wrong.js)
