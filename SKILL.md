---
name: hop
description: 在项目的任何阶段加载并始终生效：架构设计、制定计划、编写代码、重构优化、代码审查、调试修复、文件整理。确保所有代码产出遵循 HOP（面向人类编程）思想：业务逻辑为主角、信任数据不防御、永远做减法、凭直觉可掌控。当提到 HOP、面向人类编程、human-oriented programming、代码可读性、注释规范、架构规范、命名规范、代码审查、代码风格、项目结构设计、代码即文档时触发。
---

# HOP —— 面向人类编程

本规范是项目的最高工程准则。所有代码产出必须以本规范为唯一衡量标准。

## 为什么需要 HOP

AI 写代码有一套根深蒂固的坏习惯：把简单的业务写成庞大的工程，到处做防御和校验让真正的业务逻辑被淹没，遇到问题只会加代码打补丁让项目越来越臃肿，过度拆分文件和函数导致改一个逻辑要碰 N 个地方。

结果：人类接手后找不到主逻辑在哪，改个东西要理解半天，不敢动。人失去了对代码的掌控力。

HOP 要扭转这件事。

## 最高目标

**让代码的复杂度等于业务的复杂度，不多一分。人凭直觉操作代码，就是安全的。**

## 核心思想

### 1. 业务逻辑是绝对主角

打开任何一个文件，第一眼看到的就是业务在做什么。不是类型定义，不是校验逻辑，不是辅助函数，不是抽象层。业务逻辑必须占据代码的主体视觉空间。

如果把一个文件里的非业务代码全删掉，剩下的业务逻辑不到总代码的一半，说明文件已经偏离了 HOP。

### 2. 信任数据，不做防御

代码信任传入的数据，直接使用。不堆砌类型定义、格式校验、null 检查、try-catch 包裹一切。数据有问题就让它自然报错——报错本身就是最好的定位手段。

当代码全是业务逻辑时，人一眼就能看出哪里不对。防御代码越多，业务逻辑越难找，人反而更容易改错。

### 3. 永远做减法

代码是负债不是资产。代码越少，人越容易掌控。

遇到问题时的优先级：
1. 删除——产生问题的代码能不能直接不存在？
2. 重写——能不能用更少的代码重新表达？
3. 合并——分散的逻辑能不能收拢到一处？
4. 加代码——以上都不行，才允许新增。

修复后代码总行数应持平或减少。不写未来可能需要的东西。不拆不需要拆的。不抽象不需要抽象的。能一个文件搞定就一个文件搞定。

### 4. 直觉即正确

符合 HOP 的代码必须通过直觉测试：

- 直觉找到——想改某个功能，凭直觉就知道去哪个文件
- 直觉看懂——打开文件，瞟一眼就知道在干什么
- 直觉改对——觉得应该这么改，改完就是对的，不牵连其他地方
- 直觉加减——加功能写好放进去就生效，删功能删掉文件就消失

如果操作代码时需要小心翼翼、需要先搞清楚全局才敢动，说明架构有问题。改架构，不加保护代码。

## 架构主线

一切业务流程按统一链条组织：**触发 → 指令 → 数据 → 反馈**

- 触发入口只负责接收事件、调用对应指令，不做业务
- 指令只负责业务动作：读数据、处理、改数据、返回结果。指令不操作 UI
- 数据集中存放，结构显式可见
- 反馈自动发生（响应式 UI、返回值、日志等）

所有功能走一样的路径。新人从入口顺着链条追踪，不超过 3 跳就能理解完整流程。

## 零注册架构

新增模块：按约定格式写好，放到对应目录，启动时自动扫描生效。
删除模块：把文件删掉，自动消失。
不维护手动注册表、不硬编码 import 列表、不写集中配置。

判断标准：新增或删除一个功能模块后，如果需要修改该模块之外的任何文件，说明架构违反了零注册原则。

## 命名

一个核心规则：**不重复上下文已经提供的信息。**

- `Context.build()` 而不是 `Context.buildContext()`
- `Task.getById()` 而不是 `Task.getTaskById()`
- 文件在 `commands/` 下，方法不需要再叫 `xxxCommand`

用业务词汇命名，禁止技术术语（controller、service、manager、handler、helper、factory）。

缩写整体保留不变形。详细规则参见 [references/naming.md](references/naming.md)。

## 注释

注释让不懂代码的人也能读懂每一步在做什么。说业务含义，不复述语法。

- 不允许连续超过 2 行无注释的代码
- 尾随注释对齐形成双栏阅读
- 函数之间用段落标题 `// --- 动作描述 ---` 分隔
- 禁止 JSDoc 标注格式，用大白话说清楚

详细格式参见 [references/comments.md](references/comments.md)。

## 不做的事

以下行为直接违反 HOP，在任何情况下都不允许：

- 为了"解耦"把一个文件能搞定的事拆成多个文件
- 为了"防御"给函数加类型校验、null 检查、格式验证
- 为了"可能复用"提前把逻辑抽象成工具函数
- 为了"职责分离"创建只用一次的辅助函数让读者频繁跳转
- 为了"规范"写大量类型定义和接口声明
- 遇到 bug 直接加代码修补而不思考能否消除问题产生的条件
- 命名里重复上下文已经表达的信息

## 正反对比

以下文件展示了同一功能的正确写法与错误写法：

- 正确示例（JS）：[examples/correct.js](examples/correct.js)
- 错误示例（JS）：[examples/wrong.js](examples/wrong.js)
- 正确示例（Python）：[examples/correct.py](examples/correct.py)

## 自查

每次生成或修改代码后确认：

1. 打开文件，第一眼看到的是业务逻辑还是定义/校验/工具函数？
2. 非业务代码是否超过了总代码的一半？
3. 代码总行数是持平/减少了，还是增加了？增加了的话，有没有减少的方案？
4. 改一个逻辑是否只需要改一个地方？是否牵连了其他文件？
5. 命名是否重复了上下文已有的信息？
6. 新人从入口追踪到完整链条，是否不超过 3 跳？
7. 新增的模块是否放进去就能生效，不需要在其他文件注册？

发现偏离时：指出偏离点，给出让代码变少的修正方案，执行修正。