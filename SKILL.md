---
name: hop
description: 在项目的任何阶段加载并始终生效：架构设计、制定计划、编写代码、重构优化、代码审查、补注释、调试修复、文件整理。确保所有代码产出遵循 HOP（面向人类编程）思想。当提到 HOP、面向人类编程、human-oriented programming、代码可读性、注释规范、架构规范、命名规范、代码审查、代码风格、项目结构设计、代码即文档、可维护性时触发。
---

# HOP 规范

## 核心目标

代码要让人一眼看懂在做什么。人打开文件就知道这段代码的作用，想改就直接改，改完不担心会出问题。

HOP 的唯一目标：人可以凭直觉控制代码。

---

## 规范说明

每条规范包含三部分：
- 思想：解释为什么要这样做
- 做法：说明具体怎么做
- 示例：展示正确的做法是什么样子

---

## 规范内容

### 1. 业务逻辑优先

#### 思想
人打开代码是为了看业务做什么。文件里应该全是业务逻辑，不应该有其他东西。数据转换、异常处理、空值检查这些不是业务逻辑，不应该混在在业务代码之间。

#### 做法
整个项目的架构要保证业务代码是纯粹的。非业务的代码要么用库和框架解决，要么设计扩展成一个可以被项目多处代码依赖使用的通用工具函数独立放到工具函数文件夹里。业务代码文件里只能看到业务步骤。

#### 示例
一个发送消息的函数，打开后只看到四个方法调用的代码：
- 存储用户发送的消息
- 调用模型获取回复内容
- 发送回复给用户
- 更新对话状态

文件里看不到任何检查、转换、兜底的代码。

---

### 2. 解决根本

#### 思想
遇到问题时，大部分人会写代码去处理这个问题。正确的做法是改变架构，让问题不会发生。问题不发生了，代码自然就少了。项目应该越做代码越少。

#### 做法
出现 bug 时，先找到 bug 出现的原因。改代码或者改架构局部，让 bug 不可能出现。不要写补丁代码和胶水逻辑。只有增加功能才应该增加代码，修复问题不应该增加代码。

#### 示例
一个函数有时会收到 null 导致崩溃。错误做法是写代码判断参数是不是 null。正确做法是找到为什么会传入 null，改上游的代码让它不会传 null。或者重新设计整个数据流，让 null 本身就是一个正常的值，所有地方都能处理 null。

---

### 3. 信任数据

#### 思想
项目内部的代码之间要互相信任。函数接收数据后直接使用，不检查、不判断、不处理异常。如果数据有问题，程序报错就是最好的提示。代码全是业务逻辑时，人看报错就能知道问题在哪。

#### 做法
内部函数之间传递数据不做任何检查。不判断参数类型，不判断参数是否为空，不写 try-catch。只在系统边界做检查，边界是指：对外提供的 API、用户输入、外部系统返回的数据。边界检查只做一次，数据进入系统后所有内部代码都信任这个数据。

#### 示例
`Agent.send()` 是对外的 API，检查用户传入的参数不为空。内部的 `Loop.run()` 调用 `Context.build()` 时，直接传数据，不做任何检查。`Context.build()` 接收到数据后直接使用，不判断数据是否有效。

---

### 4. 逻辑集中

#### 思想
理解一个功能应该只需要看一个地方。人的脑子记不住"这部分逻辑在 A 文件，那部分在 B 文件，配置在 C 文件"。相关的代码必须写在一起，看完一个文件就能理解全部逻辑。

#### 做法
一个功能的全部代码写在一个文件里。如果某段代码只用一次，就写在使用的地方，不要提取成单独的函数。只有多个地方都要用同一段代码时，才提取出去。

#### 示例
对话界面发送消息时需要检查输入不能为空。这个检查只在发送时用一次，就直接写在发送函数里，不要提取成 `validateInput()` 函数。

---

### 5. 极简命名

#### 思想
代码要尽可能简洁。能用一个词就不用两个词。通过代码的层级关系表达所属关系，不要在名字里重复说明。

#### 做法
命名只用一个词。方法属于某个类，就只说方法做什么，不带类名。变量不加类型后缀。文件不重复目录名。通过调用方式 `主体.方法` 来表达所属关系。也尽可能的使用一个单词，不要用多个单词组成。

#### 示例
- 写 `Context.build()` 不写 `Context.buildContext()` 不写 `userContext.build()`
- 写 `messages` 不写 `messageList` 
- 写 `tool.handle` 不写 `tool_handle`
- 写 `views/Chat.vue` 不写 `views/ChatView.vue`
- 即使有多个类都有 build 方法，也都只叫 `build()`，通过 `Context.build()` 和 `Agent.build()` 区分

---

### 6. 修改安全

#### 思想
人觉得应该在某处改代码，改完就应该生效，不应该还需要去其他地方同步修改。如果改一个地方需要动多个文件，说明架构有问题。代码要轻到人敢删、敢改，改完不担心。

#### 做法
让每次修改只影响一个局部范围。删除一个功能时，只删一处代码就完成。修改一个功能时，只改一个文件。如果发现改一处需要同步改其他地方，就重新设计架构。

#### 示例
要删除"消息敏感词过滤"功能。只需要删除 `filterSensitiveWords()` 函数和调用它的那一行代码，功能就被完全删除。不需要去删配置、删注册、删其他文件里的相关代码。如果还需要去其他地方删代码，说明架构设计错了。

---

### 7. 单一来源

#### 思想
新增东西应该是简单的。加一个东西只需要声明一次，系统自动发现和使用。修改一个东西只需要改一处，变化自动生效到所有使用的地方。架构应该让正确的操作变简单，让错误的操作变困难。

#### 做法
用统一的数据结构做单一信息来源。下游需要数据时从来源提取，来源的结构变化时下游自动适应。用约定而不是注册，文件按约定放到指定位置，系统自动发现和使用。

#### 示例
项目里添加工具。写一个工具类放到 tools 目录，系统自动识别和加载。删除工具时从目录移走即可，不需要手动注销。数据结构 `history` 和 `messages` 保持相似，`Context.build()` 从 `history` 提取 `messages`，后续 `history` 增加字段时，`messages` 提取逻辑不需要改动。

---

## 判断标准

代码给人的感觉应该像小项目。项目再大也要写得像小项目一样简单。打开文件只有业务逻辑，改功能只需要改一个地方，读代码像读流程一样顺畅。代码给人这种感觉，就是对的。
---

## 参考

当你需要具体的格式指引时：

- 架构组织：[references/architecture.md](references/architecture.md)
- 命名规则：[references/naming.md](references/naming.md)
- 注释格式：[references/comments.md](references/comments.md)
- 正反代码对比：[examples/correct.js](examples/correct.js) · [examples/wrong.js](examples/wrong.js) · [examples/correct.py](examples/correct.py)
