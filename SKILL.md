---
name: hop
description: 在项目的任何阶段加载并始终生效：架构设计、制定计划、编写代码、重构优化、代码审查、补注释、调试修复、测试验证、文件整理、性能优化、安全检查、文档撰写。确保所有代码产出始终遵循 HOP（面向人类编程）思想：业务逻辑为绝对主角、信任数据不防御、永远做减法、代码复杂度等于业务复杂度、凭直觉可掌控。当提到 HOP、面向人类编程、human-oriented programming、代码可读性、注释密度、注释规范、架构规范、命名规范、代码审查、代码风格、项目结构设计、初学者友好、代码即文档、可维护性时触发。
---

# HOP —— 面向人类编程

## 这是什么

HOP 是 AI 时代的编程思想。AI 负责写代码，但人类必须能凭直觉掌控代码——看得懂、找得到、改得对、不怕动。

一个产品的真实业务逻辑往往很简单。HOP 要求代码忠实反映这种简单。

## 我们遵循的思想

### 代码复杂度 = 业务复杂度

业务有多简单，代码就必须多简单。不允许代码比业务更复杂。如果一个简单的业务被写成了看起来很大的项目，那就是失败的。

### 业务逻辑是绝对主角

打开任何一个文件，第一眼看到的必须是业务在做什么。类型定义、校验逻辑、辅助函数、抽象层——这些都不是主角，它们不能抢占业务逻辑的视觉空间。人打开文件就是为了看业务，让他第一眼就看到。

### 信任数据，不做防御

代码信任传入的数据，直接使用。数据有问题时让它自然报错——报错本身就是最好的定位手段。不需要保护代码不出错，只需要保护人能看懂。人看懂了自然不会出错。防御代码越多，业务逻辑越难找，人反而更容易改错。

### 永远做减法

代码是负债不是资产。每多一行代码，人的掌控力就弱一分。遇到任何问题，第一反应是"怎么让代码变少"，而不是"加什么代码来修"。

### 直觉即安全

人凭直觉操作代码，结果就是正确的。想改哪里就改哪里，改完不会牵连其他地方。想加功能就写好放进去，想删功能就把文件删掉。如果人按直觉操作会出错，那是架构的问题，不是人的问题。

### 零注册、即插即用

新增一个模块：按约定格式写好，放到对应目录，自动生效。删除一个模块：把文件删掉，自动消失。不去其他文件里注册、配置、import。

---

## 如何落地这些思想

理解了上面的思想后，在实际写代码和改代码时，用以下方式思考：

### 写任何代码之前

问自己："一个完全不了解这个项目的人，打开这个文件后，他能在 5 秒内看出这个文件在做什么业务吗？"

如果不能，说明非业务的东西太多了。把它们去掉或压到最少。

### 遇到 bug 需要修复时

不要立即写修复代码。先想：这个问题为什么会发生？它依赖什么前提条件？如果把这个前提条件去掉，问题还会存在吗？

如果去掉前提后问题消失了，修复方式就是去掉前提——代码变少了，问题也没了。如果你的修复让代码变多了，停下来重新想：有没有让代码变少的同时解决问题的方式？

### 想要加校验或防御代码时

问自己：如果把这个校验删掉，功能还能正常工作吗？如果能，那它就是多余的。

当代码全是业务逻辑时，人一眼就能看出哪里不对。校验代码越多，业务越难找。

### 想要拆分文件或提取函数时

问自己：拆了之后，读这段代码的人需要跳到别的地方才能理解当前逻辑吗？

如果需要跳转才能理解，就不该拆。就地写清楚，保持阅读连续。只有被多处复用并且拆了两边都更清晰时才拆。只用一次的逻辑，永远就地写。

### 给函数或变量取名时

读出完整的调用语句，看同一个信息出现了几次。`Context.buildContext()` 里 "context" 出现了两次——多余，应该是 `Context.build()`。文件名、模块名、目录名已经说了"我是谁"，方法名只说"做什么"。

### 决定项目结构时

问自己：加一个新功能模块后，需要去其他文件里做修改吗？如果需要，架构有问题。写好放进去就该自动生效。

### 写完代码回头看时

问自己：一个人凭直觉觉得应该在这里改某个逻辑，他改完会出问题吗？如果会——不是他的错，是架构的错。修正架构让直觉操作安全，不加保护代码。

---

## 架构主线

所有业务流程走统一路径：**触发 → 指令 → 数据 → 反馈**

新人从入口顺着链条追踪，不超过 3 跳理解完整流程。详见 [references/architecture.md](references/architecture.md)。

## 命名

不重复上下文已有的信息。用业务词汇，不用技术术语。详见 [references/naming.md](references/naming.md)。

## 注释

让不懂代码的人也能读懂每一步的业务含义。说做了什么，不复述语法。详见 [references/comments.md](references/comments.md)。

## 正反对比

- 正确示例（JS）：[examples/correct.js](examples/correct.js)
- 错误示例（JS）：[examples/wrong.js](examples/wrong.js)
- 正确示例（Python）：[examples/correct.py](examples/correct.py)
