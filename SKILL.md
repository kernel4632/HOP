---
name: hop
description: 在项目的任何阶段加载并始终生效：架构设计、制定计划、编写代码、重构优化、代码审查、补注释、调试修复、文件整理。确保所有代码产出遵循 HOP（面向人类编程）思想。当提到 HOP、面向人类编程、human-oriented programming、代码可读性、注释规范、架构规范、命名规范、代码审查、代码风格、项目结构设计、代码即文档、可维护性时触发。
---

# HOP —— 面向人类编程

## 精神

一个产品的业务逻辑往往很简单。代码应该忠实反映这种简单。

好的代码给人的感觉是：像一份菜谱——扫一眼就知道做什么菜、用什么材料、几步做完。打开文件就看到业务在做什么，想改就改，改完不出事。轻盈、清澈、一目了然。

HOP 的方向只有一个：**人凭直觉就能掌控代码。**

---

## 方向

**让业务逻辑占据画面的主体。** 人打开文件是为了看业务做了什么。让他第一眼就看到。其他所有东西——如果存在的必要性不够强——让它消失。

**遇到问题，让代码变少。** 想想问题为什么会发生，能不能消除它发生的条件。当条件不存在了，问题自然消失，代码也随之减少。让项目始终保持轻盈。

**信任数据，直奔业务。** 数据来了就用。真有问题时自然会报错，报错本身就是最好的定位线索。当代码全是业务逻辑时，人一眼就能看出哪里不对。

**让相关的逻辑待在一起。** 理解一件事只需要看一个地方，不需要在多个文件间跳来跳去拼凑全貌。只有真正被多处使用、分开后两边都更清晰时，才值得分开。

**方法名只说"做什么"。** 文件名、模块名、目录名已经说了"我是谁"。`Context.build()`，不需要再说一遍 context。读出调用语句，同一个信息只出现一次。

**让直觉操作是安全的。** 人觉得应该在这里改，改了就是对的。如果按直觉改会出问题，调整架构让直觉变得安全。代码轻到人敢动、改了不担心。

**放进去就生效，删掉就消失。** 新模块写好放到对应目录就能工作，删掉就不存在了。加减功能像插拔 U 盘，不需要到别处做任何登记。

---

## 感觉对了就对了

代码给人的感觉应该像小项目——即使项目实际很大，每个局部依然轻盈。打开一个文件，业务逻辑是画面里的主角。改一个功能只碰一个地方。读代码像读故事——顺着读下去就懂了。

如果你写出来的代码给人这种感觉，那就对了。

---

## 参考

当你需要具体的格式指引时：

- 架构组织：[references/architecture.md](references/architecture.md)
- 命名规则：[references/naming.md](references/naming.md)
- 注释格式：[references/comments.md](references/comments.md)
- 经典原则（KISS / YAGNI / 最小惊讶 / 零注册等）：[references/principles.md](references/principles.md)
- 正反代码对比：[examples/correct.js](examples/correct.js) · [examples/wrong.js](examples/wrong.js) · [examples/correct.py](examples/correct.py)
